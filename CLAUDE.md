간단한 개인 가계부 웹앱을 만들어줘.

목표:
- 혼자 쓰는 가계부 MVP를 만든다.
- 너무 복잡하지 않게, 바로 실행 가능한 수준으로 구현한다.
- UI는 깔끔하고 직관적으로 만든다.
- 한국어 UI로 제공한다.

기술 요구사항:
- 프론트엔드 단독으로 동작하는 형태로 만들어줘.
- 가능하면 React 기반으로 구현해줘.
- 데이터는 우선 localStorage에 저장해줘.
- 별도 서버나 로그인 기능은 넣지 말아줘.
- 코드 구조는 이후 기능 확장이 가능하도록 컴포넌트를 분리해줘.

반드시 구현할 기능 범위:
1. 필수 기능
- 수입/지출 내역 입력
- 입력 항목: 날짜, 구분(수입/지출), 금액, 카테고리, 메모
- 카테고리 관리
  - 기본 카테고리 예시:
    - 지출: 식비, 교통, 쇼핑, 주거, 통신, 의료, 여가, 기타
    - 수입: 월급, 용돈, 환급, 기타
  - 사용자가 카테고리를 추가할 수 있게 해줘
- 내역 목록 조회
  - 최신순 정렬
  - 전체 / 수입 / 지출 필터
  - 카테고리 필터
- 합계 계산
  - 이번 달 총수입
  - 이번 달 총지출
  - 이번 달 잔액

2. 있으면 좋은 기능
- 자동 통계
  - 이번 달과 지난 달 지출 비교
  - 이번 달과 지난 달 수입 비교
- 카테고리별 지출 비율 표시
  - 퍼센트와 금액을 함께 보여줘
- 검색 기능
  - 메모 또는 카테고리 기준 검색 가능하게 해줘
- 반복 지출/수입 자동 등록
  - 예: 월세, 넷플릭스, 월급
  - 반복 주기: 매월
  - 사용자가 반복 항목을 등록하면, 해당 월에 자동 반영되도록 해줘

3. 준고급 기능
- 예산 설정
  - 월별 카테고리 예산 설정 가능
  - 예: 식비 50만원
  - 예산 사용률 표시
  - 예산 초과 시 시각적으로 경고 표시
- 태그 기능
  - 거래마다 태그를 여러 개 붙일 수 있게 해줘
  - 예: 데이트, 출장, 가족, 고정비
- 지출 패턴 분석
  - 요일별 지출 합계
  - 카테고리별 월간 지출 추이
- CSV 내보내기
  - 전체 거래내역을 CSV 파일로 다운로드 가능하게 해줘

화면 구성:
1. 대시보드
- 이번 달 총수입 / 총지출 / 잔액 카드
- 지난 달 대비 증감 표시
- 카테고리별 지출 비율
- 예산 현황
- 간단한 패턴 분석 요약

2. 거래 입력 화면 또는 입력 카드
- 날짜
- 수입/지출 선택
- 금액
- 카테고리 선택/추가
- 메모
- 태그 입력
- 반복 여부 선택
- 저장 버튼

3. 거래 목록 화면
- 거래 리스트 테이블 또는 카드
- 필터: 기간, 수입/지출, 카테고리
- 검색창
- 수정 / 삭제 가능

4. 예산 관리 화면
- 카테고리별 월 예산 설정
- 사용 금액 / 잔여 금액 / 초과 여부 표시

5. 반복 항목 관리 화면
- 반복 수입/지출 목록
- 수정 / 삭제 가능

데이터 구조 요구사항:
- transaction
  - id
  - date
  - type (income / expense)
  - amount
  - category
  - memo
  - tags (배열)
  - isRecurring
  - recurringId(optional)
  - createdAt
- category
  - id
  - name
  - type
- budget
  - id
  - month
  - category
  - limitAmount
- recurringItem
  - id
  - type
  - amount
  - category
  - memo
  - tags
  - dayOfMonth

구현 규칙:
- 금액은 숫자만 입력 가능하게 하고 천 단위 구분 표시를 해줘
- 잘못된 입력에 대한 검증을 넣어줘
- localStorage 저장/불러오기 유틸을 분리해줘
- 컴포넌트와 유틸 함수를 너무 한 파일에 몰아넣지 말아줘
- 샘플 데이터 몇 개를 기본 제공해줘
- 반응형 UI로 만들어줘
- 다크모드까지는 필요 없지만 깔끔한 카드형 디자인으로 해줘

분석/통계 표시 방식:
- 별도 라이브러리를 써도 되지만 과하지 않게 해줘
- 차트는 최소한으로만 사용하고, 숫자 요약이 잘 보이게 해줘
- 패턴 분석은 아래 정도로 보여줘:
  - 가장 지출이 많은 카테고리
  - 가장 많이 지출한 요일
  - 이번 달 고정지출 추정

예외 처리:
- 데이터가 없을 때 빈 상태 UI 보여줘
- 이번 달 데이터가 없어도 에러 없이 0원으로 표시
- 반복 항목이 중복 생성되지 않도록 처리
- CSV 다운로드 시 헤더 포함

최종 결과물:
- 바로 실행 가능한 전체 코드
- 폴더 구조 포함
- 실행 방법 포함
- 필요한 경우 설치 패키지 명시
- 중요한 로직에는 주석 추가

추가 요청:
- 먼저 전체 폴더 구조와 핵심 설계 요약을 보여준 뒤,
- 그 다음 실제 코드를 파일별로 모두 작성해줘.
- 중간 생략 없이 작성해줘.